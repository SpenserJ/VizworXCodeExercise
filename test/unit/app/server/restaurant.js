/* eslint-env mocha */
import { assert } from 'chai';

import Restaurant from '../../../../app/server/restaurant';

describe('Restaurant', () => {
  it('should instantiate with an object properly', () => {
    assert.fail(false, false, 'No tests are written yet');
  });
});
