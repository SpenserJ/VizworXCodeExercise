/* eslint-env mocha */
import { assert } from 'chai';

describe('Brute Force', () => {
  it('should fail until a test is written', () => {
    assert.fail(false, false, 'No tests are written yet');
  });
});
